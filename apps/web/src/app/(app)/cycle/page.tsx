"use client";

import { CycleView } from "@/components/cycles/CycleView";

/** `/cycle` — the single cycle-management surface (slice 011; Clarifications). */
export default function CyclePage() {
  return <CycleView />;
}
