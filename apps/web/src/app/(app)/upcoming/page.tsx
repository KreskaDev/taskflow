import { UpcomingView } from "@/components/tasks/UpcomingView";

/** The Upcoming route (slice 005, served by `G U`). Mounts the keyboard-driven {@link UpcomingView}. */
export default function UpcomingPage() {
  return <UpcomingView />;
}
