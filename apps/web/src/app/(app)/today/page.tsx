import { TodayView } from "@/components/tasks/TodayView";

/** The Today route (slice 005, served by `G T`). Mounts the keyboard-driven {@link TodayView}. */
export default function TodayPage() {
  return <TodayView />;
}
