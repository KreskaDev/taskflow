import { AssignedView } from "@/components/tasks/AssignedView";

/** The "Assigned to me" route (slice 008, served by `G A`). Mounts the keyboard-driven {@link AssignedView}. */
export default function AssignedPage() {
  return <AssignedView />;
}
